A flash application that captures and encode multimedia signals (video and voice) from a source (i.e. PC) using RTMP protocol. It was written on Actionscript language.

The client can be other PCs or mobile devices on local network.
